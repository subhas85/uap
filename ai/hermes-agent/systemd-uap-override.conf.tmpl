# UAP drop-in: launch gateway from the workspace hub so ~/workspace/CLAUDE.md
# auto-loads as the Hermes project context on every Telegram session.
# Survives `hermes gateway install --system` regenerating the main unit.
[Service]
WorkingDirectory=${HOME_DIR}/workspace
